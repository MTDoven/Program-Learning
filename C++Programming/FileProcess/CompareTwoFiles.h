#pragma once

#include <string>
#include <fstream>
#include <sstream>
#include <iostream>
#include <string>

using namespace std;

string compare_strings(string str1, string str2);
string file_to_string(ifstream& fileptr);


